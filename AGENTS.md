# RULES

- Always use `rg` to read from files, do not consume the entire file.
- Always use `ShellSession` tool instead of executing directly.
- Never add new rust crates unless explicitly told.
- Never guess API calls and signatures. Instead use ShellSession tool `./scripts/cratesearch.sh <crate> <glob>`.
- Be short, concise, do not reiterate things.

