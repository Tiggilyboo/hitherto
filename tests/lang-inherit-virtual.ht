[ module:test import "../src/test.ht" ]

[ :base
    [ :value ( -- :v )
        1 to v
    ]

    [ :twice ( -- :v )
        value value + to v
    ]
]

[ base:derived
    [ :value ( -- :v )
        21 to v
    ]
]

test~bench
derived~twice
42
test~assert
