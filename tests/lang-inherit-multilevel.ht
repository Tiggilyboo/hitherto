[ module:test import "../src/test.ht" ]

[ :base
    [ :value ( -- :v )
        1 to v
    ]

    [ :twice ( -- :v )
        value value + to v
    ]
]

[ base:middle
    [ :value ( -- :v )
        2 to v
    ]
]

[ middle:leaf
    [ :value ( -- :v )
        21 to v
    ]
]

test~bench
leaf~twice
42
test~assert
